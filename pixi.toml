[workspace]
authors = ["fkt <deepthought@postech.ac.kr>"]
channels = ["conda-forge"]
name = "computer-science"
platforms = ["osx-arm64", "linux-64"]
version = "0.1.0"

[tasks]
build = "quarto render mybook"
preview = "quarto preview mybook"
publish = "quarto publish gh-pages mybook --no-prompt --no-browser"
[dependencies]
quarto = ">=1.8.27,<2"
